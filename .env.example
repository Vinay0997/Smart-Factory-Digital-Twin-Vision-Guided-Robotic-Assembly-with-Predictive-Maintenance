# Copy this file to .env in the project root before running docker compose up.
# Never commit the real .env file to GitHub - it is already covered by .gitignore.

# --- InfluxDB (time-series telemetry: OEE, vibration, cycle time, vision confidence) ---
INFLUX_USER=admin
INFLUX_PASSWORD=changeme123
INFLUX_TOKEN=replace-with-generated-token
INFLUX_ORG=smart-factory
INFLUX_BUCKET=telemetry

# --- PostgreSQL (production runs, parts, downtime events, FMEA register) ---
POSTGRES_USER=factory_admin
POSTGRES_PASSWORD=changeme123
POSTGRES_DB=production_records

# --- Grafana (OEE and predictive maintenance dashboards) ---
GRAFANA_USER=admin
GRAFANA_PASSWORD=changeme123

# --- AWS IoT Core (optional cloud forwarding from mqtt_publisher.py) ---
ENABLE_AWS_FORWARDING=false
AWS_IOT_ENDPOINT=your-endpoint.iot.us-east-1.amazonaws.com
AWS_ACCESS_KEY_ID=your-key
AWS_SECRET_ACCESS_KEY=your-secret
AWS_IOT_CERT_PATH=certs/device.pem.crt
AWS_IOT_KEY_PATH=certs/private.pem.key
AWS_IOT_CA_PATH=certs/AmazonRootCA1.pem

# --- MQTT Broker ---
MQTT_BROKER=mosquitto
MQTT_PORT=1883
